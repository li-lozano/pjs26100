---

---

<div
  class="relative w-full max-w-md space-y-6 overflow-hidden rounded-2xl border border-gray-100 bg-white p-8 shadow-xl"
>
  <!-- Decorative background elements -->
  <div
    class="absolute -top-10 -right-10 h-32 w-32 rounded-full blur-2xl"
    style="background-color: var(--color-rayito-yellow); opacity: 0.1;"
  >
  </div>
  <div
    class="absolute -bottom-10 -left-10 h-32 w-32 rounded-full blur-2xl"
    style="background-color: var(--color-rayito-blue); opacity: 0.1;"
  >
  </div>

  <div class="relative space-y-2 text-center">
    <h2
      class="text-3xl font-bold tracking-tight"
      style="color: var(--color-brand-primary);"
    >
      ¡Bienvenido!
    </h2>
    <p class="text-sm text-gray-500">
      Ingresa tus credenciales para acceder al portal
    </p>
  </div>

  <form
    id="login-form"
    action="/api/v1/auth/login"
    method="POST"
    class="relative space-y-5"
  >
    <div class="space-y-1">
      <label for="email" class="ml-1 block text-sm font-medium text-gray-700">
        Correo Electrónico
      </label>
      <div class="group relative">
        <input
          type="email"
          name="email"
          id="email"
          required
          class="block w-full rounded-xl border border-gray-200 bg-gray-50 px-4 py-3 text-gray-900 transition-all duration-200 group-hover:bg-white focus:border-cyan-600 focus:ring-2 focus:outline-none disabled:cursor-not-allowed disabled:opacity-50"
          style="focus:ring-color: var(--color-brand-primary); focus:ring-opacity: 0.2;"
          placeholder="ejemplo@correo.com"
        />
      </div>
    </div>

    <div class="space-y-1">
      <label
        for="password"
        class="ml-1 block text-sm font-medium text-gray-700"
      >
        Contraseña
      </label>
      <div class="group relative">
        <input
          type="password"
          name="password"
          id="password"
          required
          class="block w-full rounded-xl border border-gray-200 bg-gray-50 px-4 py-3 text-gray-900 transition-all duration-200 group-hover:bg-white focus:border-cyan-600 focus:ring-2 focus:outline-none disabled:cursor-not-allowed disabled:opacity-50"
          style="focus:ring-color: var(--color-brand-primary); focus:ring-opacity: 0.2;"
          placeholder="••••••••"
        />
      </div>
    </div>

    <div class="flex items-center justify-between px-1 text-sm">
      <div class="flex items-center">
        <input
          id="remember-me"
          name="remember-me"
          type="checkbox"
          class="h-4 w-4 cursor-pointer rounded border-gray-300 focus:ring-2 focus:ring-cyan-600 focus:ring-offset-0"
          style="color: var(--color-brand-primary);"
        />
        <label
          for="remember-me"
          class="ml-2 block cursor-pointer text-gray-600 select-none"
        >
          Recordarme
        </label>
      </div>
      <a
        href="#"
        class="font-medium transition-colors duration-200 hover:underline"
        style="color: var(--color-brand-primary);"
      >
        ¿Olvidaste tu contraseña?
      </a>
    </div>

    <button
      type="submit"
      id="submit-btn"
      class="group relative flex w-full transform items-center justify-center gap-2 rounded-xl border border-transparent px-4 py-3 text-white shadow-lg transition-all duration-300 hover:-translate-y-0.5 focus:ring-2 focus:ring-offset-2 focus:outline-none disabled:transform-none disabled:cursor-not-allowed disabled:opacity-50"
      style="background-color: var(--color-brand-primary); box-shadow: 0 10px 25px -5px rgba(14, 116, 144, 0.2);"
    >
      <span id="btn-text" class="relative font-bold tracking-wide"
        >Iniciar Sesión</span
      >
      <svg
        id="loading-spinner"
        class="hidden h-5 w-5 animate-spin text-white"
        xmlns="http://www.w3.org/2000/svg"
        fill="none"
        viewBox="0 0 24 24"
      >
        <circle
          class="opacity-25"
          cx="12"
          cy="12"
          r="10"
          stroke="currentColor"
          stroke-width="4"></circle>
        <path
          class="opacity-75"
          fill="currentColor"
          d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"
        ></path>
      </svg>
    </button>
  </form>

  <div class="pt-4 text-center">
    <p class="text-sm text-gray-500">
      ¿Aún no tienes cuenta?
      <a
        href="#"
        class="font-semibold transition-colors duration-200 hover:underline"
        style="color: var(--color-rayito-yellow);"
      >
        Contacta con administración
      </a>
    </p>
  </div>
</div>
