---
interface Props {
  activeTab: string;
}

const { activeTab } = Astro.props;

const tabs = [
  { id: "periodos", label: "Periodos Académicos", icon: "📅" },
  { id: "grados", label: "Grados y Secciones", icon: "🏫" },
];
---

<div
  class="flex w-fit items-center gap-2 rounded-2xl border border-gray-200 bg-gray-100/80 p-1 shadow-inner backdrop-blur-sm"
>
  {
    tabs.map((tab) => (
      <a
        href={`?tab=${tab.id}`}
        class={`flex items-center gap-2 rounded-xl px-6 py-3 font-bold transition-all duration-300 ${
          activeTab === tab.id
            ? "text-rayito-blue scale-[1.02] bg-white shadow-md ring-1 ring-black/5"
            : "text-gray-500 hover:bg-white/50 hover:text-gray-800"
        } `}
      >
        <span class="text-lg">{tab.icon}</span>
        {tab.label}
      </a>
    ))
  }
</div>
