---
interface Props {
  activeTab: string;
}

const { activeTab } = Astro.props;

const tabs = [
  { id: "periodos", label: "Periodos Académicos", icon: "📅" },
  { id: "grados", label: "Grados y Secciones", icon: "🏫" },
];
---

<div
  class="flex items-center gap-2 p-1 bg-gray-100/80 backdrop-blur-sm rounded-2xl w-fit border border-gray-200 shadow-inner"
>
  {
    tabs.map((tab) => (
      <a
        href={`?tab=${tab.id}`}
        class={`
          flex items-center gap-2 px-6 py-3 rounded-xl font-bold transition-all duration-300
          ${
            activeTab === tab.id
              ? "bg-white text-rayito-blue shadow-md scale-[1.02] ring-1 ring-black/5"
              : "text-gray-500 hover:text-gray-800 hover:bg-white/50"
          }
        `}
      >
        <span class="text-lg">{tab.icon}</span>
        {tab.label}
      </a>
    ))
  }
</div>
