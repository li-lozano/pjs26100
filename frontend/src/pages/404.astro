---
import WebLayout from "@layouts/WebLayout.astro";
import { Image } from "astro:assets";
import logo from "@assets/Logo_RDL.png";
---

<WebLayout pageTitle="Página no encontrada">
  <main
    class="relative flex min-h-screen items-center justify-center overflow-hidden bg-gray-50"
  >
    <!-- Animated background gradients -->
    <div
      class="bg-rayito-blue/10 animate-blob absolute top-0 -left-4 h-96 w-96 rounded-full opacity-70 mix-blend-multiply blur-3xl filter"
    >
    </div>
    <div
      class="bg-rayito-yellow/10 animate-blob animation-delay-2000 absolute top-0 -right-4 h-96 w-96 rounded-full opacity-70 mix-blend-multiply blur-3xl filter"
    >
    </div>
    <div
      class="bg-rayito-pink/10 animate-blob animation-delay-4000 absolute -bottom-8 left-20 h-96 w-96 rounded-full opacity-70 mix-blend-multiply blur-3xl filter"
    >
    </div>

    <div
      class="relative z-10 flex w-full flex-col items-center px-4 text-center"
    >
      <!-- Logo -->
      <div class="animate-fade-in mb-12">
        <Image
          src={logo}
          alt="Logo Rayito de Luz"
          class="h-auto w-48 drop-shadow-lg transition-transform duration-300 hover:scale-105"
        />
      </div>

      <div class="animate-fade-in-up max-w-lg space-y-6">
        <h1 class="text-rayito-blue text-9xl font-bold drop-shadow-sm">404</h1>
        <h2 class="text-3xl font-bold text-gray-800">
          ¡Ups! Página no encontrada
        </h2>
        <p class="text-lg text-gray-500">
          Parece que te has perdido en el jardín. No te preocupes, ¡vamos a
          llevarte de vuelta a casa!
        </p>

        <div class="pt-8">
          <a
            href="/"
            class="bg-rayito-blue group inline-flex items-center rounded-2xl px-8 py-4 font-semibold text-white shadow-lg transition-all duration-300 hover:scale-105 hover:bg-blue-600 active:scale-95"
          >
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="mr-2 h-5 w-5 transition-transform group-hover:-translate-x-1"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
            </svg>
            Volver al inicio
          </a>
        </div>
      </div>

      <footer
        class="mt-20 w-64 border-t border-gray-100 pt-8 text-center text-xs text-gray-400"
      >
        &copy; {new Date().getFullYear()} Jardín Infantil Rayito de Luz. <br />
        Todos los derechos reservados.
      </footer>
    </div>
  </main>
</WebLayout>

<style>
  @keyframes blob {
    0% {
      transform: translate(0px, 0px) scale(1);
    }
    33% {
      transform: translate(30px, -50px) scale(1.1);
    }
    66% {
      transform: translate(-20px, 20px) scale(0.9);
    }
    100% {
      transform: translate(0px, 0px) scale(1);
    }
  }

  @keyframes fade-in-up {
    0% {
      opacity: 0;
      transform: translateY(20px);
    }
    100% {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-blob {
    animation: blob 7s infinite;
  }

  .animation-delay-2000 {
    animation-delay: 2s;
  }

  .animation-delay-4000 {
    animation-delay: 4s;
  }

  .animate-fade-in-up {
    animation: fade-in-up 0.8s ease-out forwards;
  }

  .animate-fade-in {
    animation: fade-in-up 0.6s ease-out forwards;
  }
</style>
